import { OnApplicationShutdown } from "@nestjs/common";
import { AsyncLocalStorage } from "async_hooks";
import { ObserveModuleOptionsWithDefaults } from "../interfaces/index.js";
import { OperationTraceRegistry } from "../services/operation-trace.registry.js";
export declare class OutgoingObserveAgentService implements OnApplicationShutdown {
    private readonly options;
    private readonly logger;
    private unsubscribeHttp?;
    constructor(options: ObserveModuleOptionsWithDefaults, operationTraceRegistry: OperationTraceRegistry, asyncLocalStorage: AsyncLocalStorage<Map<any, any>>);
    onApplicationShutdown(): void;
    /**
     * Patches every copy of a driver the application can end up running on: the
     * one it resolves itself, and the one each ORM that depends on the driver
     * *directly* resolves - see `DRIVER_DEPENDENTS`.
     */
    private patchDriver;
}
